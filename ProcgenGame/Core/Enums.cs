namespace ProcgenGame.Core;

public enum InputAction
{
    ToggleFullscreen,
}

public enum TextureName
{
    Debug1 = -9,
    Border = -1,
    White = 0,
    Dirt = 1,
}

public enum FontName
{
    Default,
}